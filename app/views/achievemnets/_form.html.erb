<%= form_with(model: achievemnet) do |form| %>
  <% if achievemnet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(achievemnet.errors.count, "error") %> prohibited this achievemnet from being saved:</h2>

      <ul>
        <% achievemnet.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="field">
    <%= form.label :image_link %>
    <%= form.text_field :image_link %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
